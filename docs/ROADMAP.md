# Что осталось сделать

Актуально на апрель 2026. Миграция Supabase → Nest + Prisma **завершена**; все модули в Nest, owner-UI без Supabase, client portal работает.

---

## 1. Клиентский кабинет — дошлифовать MVP

- [ ] Reset password через новый invite/reset link
- [ ] Добавить email клиента в настройки кабинета
- [ ] Привести тексты, пустые состояния и ошибки к финальному виду
- [ ] Проверить сценарий повторной активации для существующего клиента

## 2. Напоминания — стабильный dispatch

- [ ] Вынести `POST /v1/push/reminders/dispatch` на `@nestjs/schedule` или внешний cron
- [ ] Добавить отдельные напоминания клиенту (сейчас только owner)
- [ ] Per-client настройки: offsets, quiet hours

## 3. Статистика (Charts)

- [ ] `app/api/charts/overview` — при необходимости перенести расчёты в Nest
- [ ] Убедиться, что графики (area, bar) корректно работают на реальных данных

## 4. Скидки и кампании

- [ ] Отдельная admin-страница «Скидки / Кампании»
- [ ] Таблица `discount_campaigns` + массовое назначение
- [ ] Срок действия кампании, история, статусы
- [ ] Аудитория: все клиенты / выбранные / по сегменту

## 5. Сегментация клиентов (rule-based)

- [ ] Сегмент «давно не приходил» (30 / 45 / 60 дней)
- [ ] Сегмент «был только один раз»
- [ ] Сегмент «есть скидка, но не использована»
- [ ] Быстрые действия: назначить скидку, создать кампанию

## 6. Push — улучшения

- [ ] Асинхронная очередь (BullMQ / аналог) вместо синхронного dispatch
- [ ] Retry с backoff для transient failures
- [ ] Метрики: sent / failed / skipped
- [ ] Очистка stale подписок

## 7. AI

- [ ] Embeddings: таблица `feedback_embeddings`, semantic search по отзывам
- [ ] Retrieval для рекомендаций: отбирать релевантные отзывы перед отправкой в LLM
- [ ] AI-подсказки по кампаниям (каким клиентам, какой процент, на какой срок)
- [ ] Fine-tuning dataset — только после накопления собственных данных

## 8. Auth — перспектива

- [ ] OAuth Яндекс для мастера (убрать парольный login в проде)
- [ ] Отключить `POST /v1/auth/register` в production

## 9. Инфраструктура и деплой

- [ ] Развернуть на Timeweb Cloud Server (Nginx + PM2 + PostgreSQL)
- [ ] Миграция данных из legacy (если есть продакшен на Supabase)
- [ ] SSL, cron для reminders, backup strategy

---

## Рекомендуемый порядок

1. **§1** — Client portal polish (ближайший)
2. **§2** — Cron reminders
3. **§4 + §5** — Скидки / кампании / сегменты
4. **§6** — Push improvements
5. **§7** — AI (embeddings, retrieval)
6. **§8 + §9** — Auth Яндекс, Timeweb, production
